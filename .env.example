# Database
DATABASE_URL=postgresql://todo:todo@localhost:5433/todo

# Server
PORT=3001
BIND=127.0.0.1
ALLOW_PUBLIC_BIND=false
LOG_LEVEL=info
NODE_ENV=development

# CORS — origin to allowlist for /api requests in dev (production: same-origin)
CORS_ORIGIN=http://localhost:5173
